# Dipendenze extra per l'integrazione OneDrive/hosting cloud (accantonata
# in favore dell'installazione locale). Installa insieme a requirements.txt
# solo se si riprende quella strada:
#   pip install -r requirements.txt -r requirements-onedrive.txt

msal>=1.28
requests>=2.31
gunicorn>=22.0
